h = zeros(1,64);
h(1) = 1*exp(j*2*pi*0.1);       
h(5) = 0.5*exp(-j*2*pi*0.2);
H = fft(h);
plot(abs(H));

h(2) = 0.4; h(3)=0.3; h(4)=0.1;
H = fft(h);
hold on; 
plot(abs(H));

% matlab索引为正整数，从1开始，与c++/python不同。所以诸如h(1)就是第1个元素而非h(0)
% zeros(1,64) 创建一个全零数组 / 行向量
% hold on 启动图形保持功能，当前坐标轴和图形都将保持，从此绘制的图形都将添加在这个图形的基础上，并自动调整坐标轴的范围
% hold off 关闭图形保持功能，使当前轴及图像具备被刷新的性质，新图出现时，取消原图
% plot 生成x-y坐标图